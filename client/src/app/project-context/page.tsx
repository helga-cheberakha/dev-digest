/* Route: /project-context — Project Context screen (L05).
   Thin server-component entry; interactive view is in _components/ProjectContextView. */
import { ProjectContextView } from "./_components/ProjectContextView";

export default function ProjectContextPage() {
  return <ProjectContextView />;
}
