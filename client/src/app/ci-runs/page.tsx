/* Route: /ci-runs — CI Runs page. RSC shell; all interactivity in the client view. */
import { CiRunsView } from "./_components/CiRunsView";

export default function CiRunsPage() {
  return <CiRunsView />;
}
