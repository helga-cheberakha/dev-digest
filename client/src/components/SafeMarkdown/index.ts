/* SafeMarkdown — sanitized markdown renderer for untrusted repo document content.
   No rehype-raw; javascript:/data: hrefs replaced with inert spans (AC-21). */
export { SafeMarkdown } from "./SafeMarkdown";
