# Insights — client

Non-obvious findings and gotchas. Add an entry whenever something surprised you,
so the next agent/session doesn't relearn it. Append-only — see the
`engineering-insights` skill for how entries are captured.

## What Works

- **2026-07-07** — "Resource not generated yet" tri-state: catch the 404 in the fetcher (`ApiError.status === 404` → return `null`) so TanStack Query lands in the success state with `data === null` — the page then branches cleanly: `null` → first-visit CTA, data → content, `isError` → genuine failure only. No error-object probing in components. Evidence: `client/src/lib/api.ts` (`fetchOnboarding`), `client/src/lib/hooks/onboarding.ts`.

- **2026-06-14** — `formatCost` (`src/lib/cost.ts`) distinguishes MISSING data (`null`/`undefined` → "—") from a genuine zero (`0` → "$0.00"), widens precision for sub-cent values (~2 sig figs), and trims trailing zeros to a 2dp floor ("$0.06" not "$0.060", "$0.0013" not "$0.00"). Reuse it for any per-run money display.
- **2026-07-13** — Testing a Recharts `<Tooltip content={...}>` by simulating real mouse hover in jsdom is unreliable: `ResponsiveContainer` reports 0×0 dimensions under jsdom (visible as a console warning on every chart render in this test suite already), so Recharts' internal active-index/coordinate math has no real geometry to key off, making `fireEvent.mouseOver`/`mouseMove` on the SVG a flaky way to trigger the tooltip. Pattern that works: export the tooltip's content-renderer as its own named component (e.g. `ChartTooltip({active, payload})`) and unit-test IT directly via RTL `render()` with hand-built `{active, payload}` props — bypasses the mouse-simulation problem entirely, deterministic, and still exercises the real formatting logic. Evidence: `client/src/vendor/ui/charts/LineChart.tsx` (`ChartTooltip`), `client/src/vendor/ui/charts/LineChart.test.tsx`.
- **2026-07-17** — Before building a new radial/ring gauge for a "small ring badge with a number" design (e.g. a corner badge on a summary card), check `@devdigest/ui`'s `CircularScore` primitive first (`score: number` 0-100, optional `size`/`stroke`, built-in green/amber/red threshold coloring via `--ok`/`--warn`/`--crit`) — it already covers that exact shape and is a one-line import (`import { CircularScore } from "@devdigest/ui"`), no new SVG needed. Used to fix the Agent Performance dashboard's "AVG ACCEPT RATE" card to match the design mock (`size=36`, badge positioned `absolute top/right` inside a `position:relative` card). Note this means the Stats tab's `AcceptRateGauge` (a bespoke full SVG ring built for the stats-tab-enrichment feature, `size=80`, own null-state) duplicates the same visual shape as `CircularScore` — worth consolidating onto the shared primitive next time that component is touched, rather than maintaining two ring-gauge implementations. Evidence: `client/src/vendor/ui/primitives/CircularScore.tsx`, `client/src/app/agent-performance/_components/SummaryCards.tsx`, `client/src/app/agents/[id]/_components/AgentEditor/_components/StatsTab/_components/AcceptRateGauge.tsx`.

## What Doesn't Work

- **2026-07-11** — `@devdigest/ui`'s `charts/Sparkline.tsx` (and `MetricCard`, which renders one internally whenever a `trend` prop is passed) divides by `data.length - 1` to place points — a trend array of length 1 produces `0/0 = NaN`, which React then throws into `<path d="MNaN,18.0">` / `<circle cx="NaN">` at render, spamming console errors (caught live via Playwright screenshot verification, not by the component test — jsdom doesn't surface SVG attribute-parse errors as failures). Any caller passing a real (not >=2-length-guaranteed) trend array MUST gate it: `trend={points.length >= 2 ? points : undefined}`. `AgentEvalCard`'s own local Sparkline already guarded this way; `MetricCard` usages must do the same manually since the vendor component doesn't guard internally. Evidence: `client/src/vendor/ui/charts/Sparkline.tsx`, `client/src/app/eval/[agentId]/_components/AgentEvalDetailView.tsx`.
- **2026-07-11** — A client-side "date range" filter (e.g. last 7/30/90 days) that defaults to a narrow window computed against real `Date.now()` can silently render an empty state on first load whenever the underlying data (seed/demo data, or just an agent that hasn't run recently) is older than the window — there's no visible cue that a filter, not an empty dataset, is hiding everything. Default such filters to "All time" and let the user narrow deliberately. Evidence: `client/src/app/eval/[agentId]/_components/AgentEvalDetailView.tsx` (caught before shipping — a 30-day default against 2026-07-11 "now" hid May-2026 seed batches entirely).

- **2026-07-11** — `fetchEvalDashboard()` called with no `agentId` (the null-owner/workspace variant) returns `recent_runs` as bare `EvalRunRecord[]` — per-*case* rows with no agent name/id attached. It cannot power a workspace-wide table that needs to show which agent a row belongs to (e.g. an "all agents" recent-runs view). Fix: drop the null-owner call and instead fan `fetchEvalBatches(agentId)` out across every agent via `useQueries`, tagging each returned `EvalRunBatch` with the agent's own name/id client-side, then merge+sort by `ran_at`. `fetchEvalBatches` is already agent-scoped and already used by `EvalsTab`, so this reuses a proven endpoint instead of extending the dashboard contract. Evidence: `client/src/app/eval/_components/EvalDashboardView.tsx`, `client/src/lib/api.ts:195` (`fetchEvalBatches`).
- **2026-07-07 (later)** — An inline preview panel appended AFTER a long list inside a narrow column (`maxWidth:580`) opens below the fold and reads as "the button does nothing" — the Context tabs' original `<pre>` preview was state-wired correctly yet reported broken for exactly this reason. For any on-demand content preview use the vendor kit `Drawer` (`@devdigest/ui`, `position:fixed; inset:0` right-side overlay — RunTraceDrawer precedent): visibility becomes layout-independent. Also supersedes the `SafeMarkdown` evidence path above: the component was promoted to `client/src/components/SafeMarkdown/` (old `_components/` copy deleted). Evidence: `client/src/app/agents/[id]/_components/AgentEditor/_components/ContextTab/ContextTab.tsx`.
- **2026-07-07** — `vendor/ui/primitives/Markdown` is NOT safe for untrusted (repo-controlled) content: it renders `<a href={href}>` without filtering `javascript:`/`data:` URLs. For untrusted markdown use the `SafeMarkdown` wrapper pattern (custom `a` component with an `isSafeHref` check, no `rehype-raw` so inline HTML stays inert). Evidence: `client/src/vendor/ui/primitives/Markdown.tsx:53-57`, `client/src/app/project-context/_components/SafeMarkdown.tsx`.
- **2026-07-15** — Despite the rule directly above, `FindingCard.tsx` (the single-agent PR page's finding detail view — Accept/Dismiss/Turn-into-eval-case) had shipped rendering `f.rationale`/`f.suggestion` (untrusted LLM output) through the raw `Markdown` primitive, not `SafeMarkdown` — found only by diffing it against a hand-rolled multi-agent duplicate while reusing this component elsewhere. A documented rule doesn't guarantee every existing call site follows it — when reusing/auditing a component that renders agent/LLM text, grep its own source for `<Markdown>` before trusting it's already safe. Fixed. Evidence: `client/src/app/repos/[repoId]/pulls/[number]/_components/FindingCard/FindingCard.tsx`.
- **2026-07-15** — `useRunEvents(runIds)`'s `running` return value is the AGGREGATE state across the whole batch — it only flips to `false` once every EventSource in the list has closed (`open <= 0` in the hook). Gating a query-invalidate effect on that flag (`if (!running && wasRunningRef.current) invalidate()`) means the invalidate fires only once when the SLOWEST run in the batch finishes — an agent that completed early in a multi-agent fan-out never showed its score/findings until every sibling was also done. Fix: track per-run terminal SSE events (`kind === "result" | "error"`) in a `Set<runId>` ref and invalidate the instant a NEW one appears, regardless of whether `running` is still `true` for the batch as a whole. Also don't forget the SECOND query that needs invalidating: Tabs mode's finding cards read `usePrReviews` (`["reviews", prId]`), a completely separate query from `["multi-agent-run", runId]` — invalidating only the latter left findings stuck until a full page reload even after this fix. Evidence: `client/src/app/multi-agent/[runId]/_components/MultiAgentResultsView.tsx`, `client/src/lib/hooks/reviews.ts` (`useRunEvents`).
- **2026-07-06** — The reference-build `BlastGraph` (Blast tab Graph toggle) graphed ONLY `blast.downstream[0]` and rendered the "nothing to graph" empty state whenever that first entry had 0 callers — even when later symbols had callers/endpoints (PR #7: `downstream[0]` = CodeLine with 0/0, while executeRuns/ReviewRunExecutor each had a caller). Fixed by deriving per-symbol nodes/edges over ALL downstream entries (same attribution as `BlastGraphLightbox`), skipping symbols with nothing downstream; empty state now requires zero edges total. Evidence: `client/src/app/repos/[repoId]/pulls/[number]/_components/BlastRadius/BlastRadius.tsx:96`.
- **2026-06-30** — `reviews.ts` does NOT re-export `usePrIntent` or `useSmartDiff` by default — those hooks live in `intent.ts` and `smartDiff.ts` respectively. `page.tsx` imports both from `reviews.ts` and silently fails at runtime if not bridged. Fix: add `export { usePrIntent, useClassifyIntent } from './intent.js'` and `export { useSmartDiff } from './smartDiff.js'` to `reviews.ts`. Evidence: `client/src/lib/hooks/reviews.ts`.
- **2026-06-30** — Two SmartDiffViewer implementations exist simultaneously: the reference version at `_components/SmartDiffViewer/` (props: `smartDiff, files, commenting?`) and the enhanced version at `src/components/SmartDiffViewer/` (props: `smartDiff, allFindings, files?, onNavigateToFinding`). `DiffTab` must import from `@/components/SmartDiffViewer` (the enhanced one); using the relative `../SmartDiffViewer` path would silently miss the enhanced API or fail if `_components/SmartDiffViewer/` is absent. Evidence: `_components/DiffTab/DiffTab.tsx`.
- **2026-06-30** — `PrBrief.intent` schema diverged from the reference build: current codebase uses `summary: string`; the reference uses `intent: string`. Copying `PrBriefCard` from the reference causes a TS error at `intent.intent` — replace with `intent.summary`. The mismatch also breaks test fixtures. Evidence: `_components/PrBriefCard/PrBriefCard.tsx:42`, `client/src/vendor/shared/contracts/brief.ts:10`.
- **2026-06-17** — The PR-list `tableCard` has `overflow: "hidden"` (`pulls/styles.ts`) which CLIPS absolutely-positioned hover popovers (`FindingsHoverCard`) opening downward from the bottom rows; upper rows render fine (matching the design). `FindingsHoverCard` is dependency-free (anchor wrapper + `position:absolute` panel) — to fully escape the card it would need a portal + `position:fixed` from the anchor's `getBoundingClientRect`. Deferred; not needed for the common case. Evidence: `client/src/components/FindingsHoverCard/`, `pulls/styles.ts:97`.
- **2026-07-15** — A page built to always receive a pre-seeded param (e.g. `ConfigureRunView`'s `?prId=`, originally only reachable via the PR-page picker's "Configure agents…" link) becomes a real dead-end the moment ANYTHING else links to it cold — here, adding the design's own sidebar nav entry for the page. The page had no PR-picker UI at all, just a static "No PR selected" message once cold-landed. The design source (decoded from the standalone HTML export) had a real interactive `Dropdown` for this the whole time — the spec's prose (AC-6/AC-7) only described the two display states, not the selection mechanism, so this was invisible to a spec-only read. When wiring a NEW entry point to an existing page, check whether the page still works when every one of its optional/pre-seeded inputs is absent — don't assume the existing states are exhaustive. Evidence: `client/src/app/multi-agent/configure/_components/ConfigureRunView/ConfigureRunView.tsx`.

- **2026-07-11** — `EvalRunBatch`/`EvalDashboard.current.traces_passed`/`traces_total` count per-*case* runs, NOT per-region/finding — a batch of 3 cases has `traces_total: 3` regardless of how many findings each case's `expected_output.regions[]` lists. `runEvalCase(caseId)` (the modal's "Run case" / a row's run icon) generates its OWN isolated `batchId` (see `service.ts` `runCaseOnce`), so it becomes its own 1-trace batch — and since the dashboard's `current`/`delta` are "latest batch vs. previous batch", running ONE case standalone temporarily makes the EvalsTab metrics strip reflect only that single case's recall/precision (e.g. 100%) instead of the aggregate across all cases, until the next full "Run all evals" batch. Confirmed live via Playwright screenshot. Evidence: `server/src/modules/eval/service.ts` (`runCaseOnce`), `client/src/app/agents/[id]/_components/AgentEditor/_components/EvalsTab/EvalsTab.tsx`.
- **2026-07-11** — `EvalDashboard.delta.*` defaults to `0` server-side when there is no prior batch to compare against (`analytics.ts`: `current && prev ? current.x - prev.x : 0`) — a UI that renders any non-null delta as a flat/unchanged badge misreads "no baseline yet" as "genuinely unchanged". Gate delta rendering on `dashboard.trend.length >= 2` (a real prior point exists), not just `dashboard != null`. Evidence: `client/src/app/agents/[id]/_components/AgentEditor/_components/EvalsTab/EvalsTab.tsx`.
- **2026-07-11** — `diff-viewer/helpers.ts`'s `parsePatch()` checks only `raw.startsWith("-")`/`"+"`, so it misclassifies unified-diff file-header lines (`--- a/file`, `+++ b/file`) as `del`/`add` content lines — it's built for hunk bodies only, not a full diff blob with headers. A read-only preview of a raw `input_diff` string (headers + hunks) needs its own line classifier that checks `--- `/`+++ ` prefixes before the `+`/`-` checks. Evidence: `client/src/components/diff-viewer/helpers.ts`, `client/src/components/EvalCaseModal/EvalCaseModal.tsx` (`parseDiffLines`).
- **2026-07-13** — `EvalCaseModal`'s region-row red/invalid styling (`isRowInvalid`) reused the SAME validity check for both "disable Save" and "render error border" — since a fresh blank region row (`file: ""`) is invalid by definition, the New Eval Case modal opened with its lone default row already outlined in red, before any user input. Fix: give `RegionRow` a stable `id` (`crypto.randomUUID()`, not array index) and track a `touchedRegionIds` Set populated inside `updateRegion`; gate the red styling on `isRowInvalid(row) && touchedRegionIds.has(row.id)` while Save-disabling still uses the raw, untouched-agnostic `isRowInvalid`. General pattern: a "some field is required" validity check that also drives visual error styling needs a separate touched/dirty gate, or every fresh/blank row renders as broken on load. Evidence: `client/src/components/EvalCaseModal/EvalCaseModal.tsx` (`isRowInvalid`, `touchedRegionIds`).
- **2026-07-15** — Before this date, the app had ZERO mobile/responsive support ANYWHERE — no `@media` breakpoint touches layout, no `useMediaQuery`/`matchMedia` usage, `vendor/ui/shell/Sidebar.tsx` is a fixed `width:264` always rendered in-flow. Confirmed live (real browser at 390px, both the multi-agent-review page AND the unrelated PR-list page) — identical breakage on every page: sidebar alone ate ~2/3 of the viewport, squeezing everything else into ~126px, wrapping titles into 3+ lines. This is a global `AppFrame`/`Sidebar` gap, not a page-specific one — never try to fix mobile layout on ONE page in isolation, the sidebar dominates every page equally until fixed at the shell level. Fixed via `vendor/ui/shell/useIsMobile.ts` (matchMedia, breakpoint 880px) + `AppFrame.tsx` (below breakpoint, Sidebar renders as a `position:fixed` off-canvas overlay with a click-to-close backdrop, toggled by a new hamburger `IconBtn` in `Topbar.tsx`; auto-closes by watching `ctx.activeKey` change after navigation) — desktop layout is untouched (same fixed in-flow Sidebar) above the breakpoint. Evidence: `client/src/vendor/ui/shell/AppFrame.tsx`, `Topbar.tsx`, `useIsMobile.ts`.
- **2026-07-15** — Even after the sidebar fix above, a fixed-track CSS grid (`gridTemplateColumns: repeat(n, minmax(220px, 1fr))`, e.g. multi-agent Columns mode) still overflows a narrow (~350-390px) content width — 2+ tracks at a 220px floor don't fit. It doesn't break the page (the `<main>` ancestor already has `overflow:auto`, so it becomes a horizontal-scroll region, confirmed working via `main.scrollLeft`), but there's no visible scroll affordance/hint on a phone — a real fix would need either an explicit horizontal-scroll-snap treatment or collapsing to a single-column stack below a content-width breakpoint. Deferred — out of scope for the initial sidebar fix (which was the actual blocker), noted for a follow-up if narrow-viewport grids need to look intentional rather than just "technically scrollable." Evidence: `client/src/app/multi-agent/[runId]/_components/MultiAgentResultsView.tsx` (Columns-mode grid).

## Codebase Patterns

- **2026-07-15** — A new top-level route's `page.tsx` renders with NO sidebar/topbar at all if its client view forgets to wrap itself in `<AppShell>` — `app/layout.tsx` (the Next.js root layout) does NOT provide the shell; every leaf page opts in individually, in every return branch (loading/error/success), not just the happy path. Caught via screenshot: `multi-agent/[runId]/_components/MultiAgentResultsView.tsx` shipped without the wrap while its sibling `multi-agent/configure/_components/ConfigureRunView.tsx` had it correctly. A page missing the wrap also silently loses the max-width the sidebar's presence normally forces: cards using `flex:1` with no `maxWidth` container stretch to the full viewport width, reading as "broken" layout once real (non-empty) content is present — cap such grids with e.g. `gridTemplateColumns: "repeat(auto-fill, minmax(240px, 300px))"` + `justifyContent: "flex-start"` inside a `maxWidth`-bounded wrapper, not bare `flex:1`. Evidence: `client/src/app/multi-agent/[runId]/_components/MultiAgentResultsView.tsx`, `client/src/app/multi-agent/configure/_components/ConfigureRunView/ConfigureRunView.tsx`.
- **2026-07-15** — `vendor/ui/nav.ts`'s `NAV` array does not auto-populate even when a route's translation key and active-state predicate already exist — `shell.json`'s `nav.*` namespace and `activeKeyFor()` (`components/app-shell/helpers.ts`) were pre-seeded with `memory`, `multi-agent`, `agent-performance`, `ci-runs` ahead of the pages/nav-items that use them. `multi-agent` had a route (this lesson) but no `NAV` entry until added here (new `"GLOBAL"` section, icon `Workflow`, href `/multi-agent/configure`); `memory`/`agent-performance`/`ci-runs` still have neither a route nor a NAV entry as of this writing — a future lesson building one of those pages must add BOTH the page AND the `NAV` array item, since neither happens automatically from the other. Complements the existing nav.ts/helpers.ts split note below. Evidence: `client/src/vendor/ui/nav.ts`, `client/messages/en/shell.json:17-30`, `client/src/components/app-shell/helpers.ts:29`.
- **2026-07-15** — `client/src/lib/agent-visual.ts` is the shared convention for per-agent icon + color: `agentIcon(name)` keyword-matches known personas (security/performance/test-quality/mentor/customer-facing/architect/docs/bug) to an `IconName`, defaulting to `Cpu` (matching `AgentCard`'s prior hardcoded default); `agentColor(agentId)` hashes the id into the 5 EXISTING semantic tokens (`--accent`/`--ok`/`--warn`/`--crit`/`--info` + their `-bg` pairs) rather than inventing new hex values, so per-agent color stays theme-aware. Reuse this for any future surface listing multiple agents side by side instead of a new implementation. Evidence: `client/src/lib/agent-visual.ts`.

- **2026-07-11** — List-page headers with a title+subtitle block next to a search box / action button (`AgentsListView`, now also `SkillsListView`) use `alignItems: "center"` on the outer flex row, NOT `"flex-start"` — the search box and primary button are meant to sit centered against the combined title+subtitle block's vertical midpoint, not top-aligned to the H1. Evidence: `client/src/app/agents/_components/AgentsListView/styles.ts:7` (`header`), `client/src/app/skills/_components/SkillsListView/SkillsListView.tsx`.

- **2026-07-07** — Sidebar nav is split across two files: `vendor/ui/nav.ts` only declares items/hrefs (`:repoId`-templated); the active-state predicate is `activeKeyFor` in `components/app-shell/helpers.ts` — NOT in nav.ts (plan docs mislocated it there). Two routes sharing a trailing segment (`/onboarding` add-repo wizard vs `/repos/:id/onboarding` tour) cross-highlight unless the predicate is path-specific (`pathname.includes("/repos/") && pathname.endsWith("/onboarding")`). Evidence: `client/src/components/app-shell/helpers.ts:29`, `client/src/vendor/ui/nav.ts`.

- **2026-06-25** — `DiffTab`'s smart/original toggle uses a `useRef` flag (`defaultedRef.current`) to flip the view to `"smart"` exactly once when `smartDiff` data first arrives, without re-flipping if the user switches back to original. A plain `useEffect([smartDiff])` without the ref would re-apply the default on every remount or data refresh. Evidence: `client/src/app/repos/[repoId]/pulls/[number]/_components/DiffTab/DiffTab.tsx`.
- **2026-06-25** — `navigateToFinding(findingId)` in `page.tsx` sets BOTH `tab=findings` AND `finding=<id>` in a single `router.replace` to avoid double navigation that calling `setParam` twice would cause. Evidence: `client/src/app/repos/[repoId]/pulls/[number]/page.tsx`.

- **2026-06-17** — `FindingsHoverCard` renders its panel in a `createPortal(document.body)` with `position:fixed` (coords measured from the anchor's `getBoundingClientRect` on open, recomputed on resize, closed on scroll). This is the fix for the earlier `overflow:hidden` clipping limitation — the panel escapes any clipping ancestor. Because the panel is outside the anchor's subtree, BOTH the anchor and the portal panel carry the open/close mouse handlers (shared 120ms timer) so the pointer can cross the gap. Evidence: `client/src/components/FindingsHoverCard/FindingsHoverCard.tsx`.
- **2026-06-17** — Finding deep-linking: a findings popover navigates to `…/pulls/:number?tab=findings&finding=:id`. The PR-detail page reads `?finding`, forces the findings tab, and threads `focusFindingId` → `FindingsTab` (resolves finding→run, reuses the `targetRunId` open+scroll) → `ReviewRunAccordion` (opens if it owns the finding) → `FindingsPanel` (scrolls to `[data-finding-id]` + `defaultExpanded`). A finding's file:line link opens the PR's Files tab (`githubPrFilesUrl`), not the standalone blob. Evidence: `pulls/[number]/page.tsx`, `FindingsTab`, `ReviewRunAccordion`, `FindingsPanel`.

- **2026-06-18** — `BarChart2` and `GripVertical` do NOT exist in the `@devdigest/ui` icon registry. Use `BarChart` for charts and a unicode character (e.g. `⠿`) for drag handles. Always verify icon names against `client/src/vendor/ui/icons.tsx` before using them — a wrong name silently renders nothing because Icon is a proxy object.
- **2026-06-18** — The `AgentEditor` tab system has TWO places to update: `TABS` constant in `AgentEditor/constants.ts` (controls the tab bar) and `VALID_TABS` array in `agents/[id]/page.tsx` (validates the `?tab=` URL param). Both must be kept in sync when adding a tab — missing VALID_TABS causes the new tab to silently redirect to `config`. Evidence: `client/src/app/agents/[id]/_components/AgentEditor/constants.ts`, `client/src/app/agents/[id]/page.tsx:15`.
- **2026-07-07** — `SkillEditor`'s tab registration differs from `AgentEditor`: its `TABS` array is INLINE in `SkillEditor.tsx` (no separate constants file), while `VALID_TABS` still lives in `skills/[id]/page.tsx` — adding a skill-editor tab means editing both, but don't go looking for a `constants.ts`. Evidence: `client/src/app/skills/[id]/_components/SkillEditor/SkillEditor.tsx:11`, `client/src/app/skills/[id]/page.tsx:16`.

- **2026-06-14** — Cross-route shared components live in `src/components/<Name>/` with an `index.ts` barrel, imported via `@/components/<Name>` (e.g. `RunCostBadge`, `diff-viewer`). Vendored UI primitives (`Badge`, `CircularScore`) live in `src/vendor/ui` under `@devdigest/ui` — different home. Evidence: `client/src/components/RunCostBadge/`.
- **2026-07-17** — When adding a ring/gauge to a stat card, use a `badge` prop rendered `position:"absolute", top, right` on a `position:"relative"` card — NOT a slot that renders inline below the value. StatsTab's old `MetricCard` had only an `extra` slot (`marginTop:8`, flows below the value), so its accept-rate ring stacked as a second, huge block under the "82%" text — while the Agent Performance page's `SummaryCards`' `StatCard` had already solved this correctly with an absolute-positioned `badge` corner slot + the shared `CircularScore` primitive. Fix: gave `MetricCard` the same `badge` prop/positioning and swapped in `CircularScore`; deleted the bespoke `AcceptRateGauge.tsx` (0..1-scale props, always-green fill, its own separate "No data" ring) it replaced — `CircularScore` takes a 0-100 `score` and already has color thresholds (`>=75` ok / `>=50` warn / else crit) baked in. Evidence: `client/src/app/agents/[id]/_components/AgentEditor/_components/StatsTab/StatsTab.tsx` (`MetricCard`, `CARD_STYLE`).
- **2026-06-14** — The PR-list table is driven by two parallel constants that MUST stay length-aligned: `COLUMN_KEYS` (header keys + order) and `GRID` (CSS grid-template tracks). Adding a column = add to both AND render a matching cell in `PRRow.tsx`, else header/cells misalign silently. Evidence: `client/src/app/repos/[repoId]/pulls/constants.ts`.
- **2026-06-14** — i18n has only the `en` locale (`client/messages/en/`); new UI strings need a key under the right namespace file (e.g. `prReview.json`, `runs.json`) read via `useTranslations("<ns>")`. A missing key renders the raw key, not an error.
- **2026-07-11** — `EvalsTab.tsx` (Agent Editor's Evals tab) reads its strings from `agents.json`'s `evals.*` namespace (`useTranslations("agents")`, `t("evals.…")`) — NOT from `eval.json`'s `evalsTab.*` section, which looks like the obvious home but is dead/unused (no component references it). Don't edit `eval.json`'s `evalsTab` block expecting it to affect the tab; edit `agents.json`'s `evals` block instead. `eval.json` IS used by `EvalCaseModal` (`caseEditor.*`) and the full dashboard pages (`dashboard.*`/`detail.*`/`compare.*`). Evidence: `client/messages/en/agents.json` (`evals`), `client/messages/en/eval.json` (`evalsTab` — orphaned).
- **2026-07-11** — `EvalCaseListItem.latest_run` (from `GET /agents/:id/eval-cases`) carries only `pass`/`recall`/`precision`/`citation_accuracy`/`ran_at` — no actual-finding count. To show "expected N, got M" without a contract change (the type lives in the do-not-touch `vendor/shared` mirrors), derive `got = Math.round(recall * expectedRegionCount)` — `recall` is literally defined as the fraction of expected regions found, so this is an exact reconstruction, not an approximation, and naturally yields `got: 0` for a `must_not_flag` case with 0 expected regions. Evidence: `client/src/app/agents/[id]/_components/AgentEditor/_components/EvalsTab/EvalsTab.tsx` (`rowSubtitle`).
- **2026-07-11** — `EvalCaseModal` has two call sites with different context available: `EvalsTab` (agent-scoped, always knows the agent name) and `FindingsPanel`'s finding-derived draft flow (`pulls/[number]/_components/FindingsPanel/`, no agent object in scope). Made `agentName` an optional prop with a generic i18n fallback subtitle rather than threading an extra agent fetch into `FindingsPanel` just for a modal subtitle. Evidence: `client/src/components/EvalCaseModal/EvalCaseModal.tsx`, `client/src/app/repos/[repoId]/pulls/[number]/_components/FindingsPanel/FindingsPanel.tsx`.

- **2026-07-06** — The `BlastRadius/` barrel (`index.ts`) re-exports ONLY `BlastRadiusView`. `blastCounts` and `STAT_ICONS` must be imported directly from `../BlastRadius/helpers.js` / `../BlastRadius/constants.js` — importing them from `../BlastRadius` compiles a missing-export error. Evidence: `client/src/app/repos/[repoId]/pulls/[number]/_components/BlastRadius/index.ts`.

- **2026-06-30** — `CodeLine` was a private unexported function inside `DiffViewer.tsx`. To reuse it with per-line finding highlights in SmartDiffViewer, extract it to `diff-viewer/CodeLine/CodeLine.tsx` with two extra props: `rowBackground?: string` (overrides line-kind color) and `rightBadge?: React.ReactNode` (appended after line text). Import via `@/components/diff-viewer/CodeLine`. Evidence: `client/src/components/diff-viewer/CodeLine/CodeLine.tsx`.

- **2026-07-15** — For any "N-way side-by-side comparison" card (multiple named participants' takes on the same item), the design system's pattern is a CSS grid `gridTemplateColumns: repeat(n, 1fr)` with `gap:1, background:"var(--border)"` on the grid container and each cell independently set to `background:"var(--bg-elevated)"` — the 1px gap lets the border color show through as a free divider between cells, no explicit `borderRight` needed per cell. Used for the multi-agent "Where agents disagree" conflict grid (one column per agent, including "did not flag" cells). Evidence: `client/src/app/multi-agent/[runId]/_components/MultiAgentResultsView.tsx` (`ConflictsSection`).
- **2026-07-15** — A finding/result "mini card" in a narrow column MUST put the file:line reference on its own line below the title, not on the same flex row — putting an un-truncated `flexShrink:0` file:line span on the same row as a title that has no `whiteSpace:nowrap`/ellipsis lets a long path (e.g. a deep `client/src/app/...` path) claim most of the row's width, squeezing the title into a several-characters-wide sliver that then wraps one word per line, reading as a broken layout. Card styling: `padding:"8px 10px", borderRadius:6, background:"var(--bg-surface)"` (one shade darker than the `--bg-elevated)` card it sits inside), `borderLeft:"2px solid <severity color>"` (from `SEV[severity].c`, `@devdigest/ui`). Evidence: `client/src/app/multi-agent/[runId]/_components/MultiAgentResultsView.tsx` (`ColumnCard` findings list).
- **2026-07-15** — `FindingCard` (the single-agent PR page's Accept/Dismiss/Turn-into-eval-case component) is the canonical, reusable finding-detail card — don't re-derive its Accept/Dismiss/eval-case logic in a new surface. It now takes an opt-in `showLearnReply?: boolean` prop (default off) that adds two more actions after "Turn into eval case"; both are LOCAL-ack-only (no `onAction` call, just internal `useState`) because there is no `/findings/:id/learn` or `/findings/:id/reply` server route — only `accept`/`dismiss` are registered in `FINDING_ACTIONS` (`server/src/modules/reviews/routes.ts:16,142`). Multi-agent's Tabs mode (AC-25's 5-action row) is the first consumer of `showLearnReply`. Its i18n keys (`prReview.json`: `finding.learn`, `finding.replyToAuthor`) already existed pre-seeded and unused before this. Note: `FindingCard/styles.ts` also has unused `s.composer`/`s.composerActions` alongside unused `replyPlaceholder`/`sendReply`/`cancel` i18n keys — a real Reply *composer* (text box, not just an ack button) was clearly planned; build on those instead of starting over when a real Reply backend exists. Evidence: `client/src/app/repos/[repoId]/pulls/[number]/_components/FindingCard/FindingCard.tsx`, `client/src/app/multi-agent/[runId]/_components/MultiAgentResultsView.tsx`.
- **2026-07-15** — `client/src/lib/time-ago.ts` (`formatTimeAgo`) is the shared relative-time formatter ("just now"/"5m ago"/"3h ago"/"2d ago") — extracted from a copy that used to live only inside `OnboardingTourView.tsx`. Import it instead of re-deriving the same `Date.now() - new Date(x).getTime()` bucketing a third time (used for the Configure page's "last run" banner and `OnboardingTourView`). Evidence: `client/src/lib/time-ago.ts`.
- **2026-07-15** — `RunHistory.tsx`'s timeline groups a multi-agent fan-out's N `agent_runs` (sharing a non-null `multi_agent_run_id`) into ONE batch row via `groupByMultiAgentRun` — without this, a 3-agent multi-agent run reads as 3 disconnected single-agent rows in the PR timeline with no link back to the combined results page. The batch's aggregate follows the same convention as the results page: cost = SUM, duration = MAX (AC-28), and outcome badge = worst-case across the batch (failed > running > blockers > findings > approved), reusing `outcomeOf()` per-run then picking the worst rather than inventing a separate aggregate-outcome function. Evidence: `client/src/app/repos/[repoId]/pulls/[number]/_components/RunHistory/RunHistory.tsx` (`groupByMultiAgentRun`, `batchOutcome`, `batchTotals`).
- **2026-07-15** — The multi-agent Tabs-mode summary card deliberately has NO agent icon at all (unlike every other multi-agent surface — columns, conflict rows, tab bar — which all use `agentIcon()`/`agentColor()`): per the decoded design JSX, the card identifies the agent purely by a big `color`-tinted name (`fontSize:14, fontWeight:600, color: p.color`) plus a `borderLeft: 3px solid p.color` on the card itself, with "View trace" + duration/cost stacked via `MonoLink` in a `marginLeft:"auto"` right column — NOT a bordered `Button`. Don't "fix" this card by adding the icon back for consistency with the other surfaces; the design intentionally varies icon-vs-name-color emphasis by surface. Evidence: `client/src/app/multi-agent/[runId]/_components/MultiAgentResultsView.tsx` (Tabs-mode summary card).
- **2026-07-15** — The design's Tabs-mode tab bar is an underline tab strip (flat row, `borderBottom:"1px solid var(--border)"` container, each tab's own `borderBottom: 2px solid (active ? agentColor : transparent)`, `marginBottom:-1` so the underline sits flush on the container's border), NOT boxed buttons with border+background per tab. A first pass rendered them as bordered/background buttons (looks like a segmented control) — visually wrong even though behaviorally identical; when a design shows a tab bar, check whether the "active" affordance is a background box or an underline before implementing, they read very differently. Evidence: `client/src/app/multi-agent/[runId]/_components/MultiAgentResultsView.tsx` (Tabs-mode tab bar).
- **2026-07-15** — Columns-mode `ColumnCard` design is a strict 3-section stack, not a single padded flex column: header (`padding:12, borderBottom:1px, borderTop:"2px solid <agentColor>"` — icon box + name/time·cost + a `CircularScore(32, stroke 3.5)` at the header's far right, replaced by a status pill only while running/failed), body (`padding:12`, findings only), footer (`padding:"9px 12px", borderTop:1px, background:"var(--bg-surface)"` — `MonoLink` "View trace" + findings count, NOT a bordered `Button` at the card's bottom). Design drops the provider/model line entirely (time·cost is the only meta line under the name). Grid: `repeat(min(n,5), minmax(220px, 1fr))` — columns share the row evenly up to 5, then scroll horizontally; NOT `auto-fill` with a fixed max-width + left-justify (that was a stopgap for when the page itself had no sidebar/max-width — no longer needed once the page is properly bounded). Evidence: `client/src/app/multi-agent/[runId]/_components/MultiAgentResultsView.tsx` (`ColumnCard`, Columns-mode grid).

## Tool & Library Notes

- **2026-07-15** — The standalone design HTML exports under `/Users/helga/Sites/neoversity/lessons/DevDigest Design (standalone) (N).html` do NOT contain component source as plain text — `grep`-ing for a component/string name (e.g. `ConflictsSection`, `"Where agents disagree"`) finds nothing even though the component is real and mounted. The file stores each script as a gzip+base64 blob in a big JSON map (`"<uuid>":{"mime":"application/javascript","compressed":true,"data":"<base64>"}`), referenced by `<script type="text/babel" src="<uuid>">` where the "src" is a lookup key, not a URL. To read the real JSX: regex-extract `"([0-9a-f-]{36})":\{"mime":"([^"]+)","compressed":(true|false),"data":"([^"]*)"\}` from the file, base64-decode + gunzip each match's `data`, then grep the decoded text for the component name. One blob per top-level design module (~46 blobs in a 1.7MB single-line file); the one containing a target component usually contains its whole feature slice (e.g. one blob had `ScreenMultiAgent` + `ConflictsSection` + `RunConfig` + `ColumnsView` + `TabsView` together).
- **2026-07-07** — jsdom also lacks `IntersectionObserver` (the shared test setup stubs neither it nor `ResizeObserver`) — scroll-spy components crash in tests unless the test file defines a minimal class stub on `globalThis` before render, same pattern as the ResizeObserver entry below. Evidence: `client/src/app/repos/[repoId]/onboarding/_components/OnboardingTourView/OnboardingTourView.test.tsx`.

- **2026-07-06** — `@testing-library/user-event` is NOT in client's package.json — component tests must use `fireEvent` from `@testing-library/react` for clicks/keys (established pattern: `BlastTab.test.tsx`). Don't write `userEvent.setup()` from RTL-skill muscle memory; it fails at import. Evidence: `client/src/app/repos/[repoId]/pulls/[number]/_components/OverviewTab/BlastRadiusSection.test.tsx`.
- **2026-07-06** — jsdom has no `ResizeObserver` and no global stub exists in the client test setup — any component that instantiates one (e.g. to measure an SVG canvas) crashes its tests unless the test file defines a minimal stub (`class { observe(){} unobserve(){} disconnect(){} }`) on `globalThis` before render. Evidence: `client/src/app/repos/[repoId]/pulls/[number]/_components/OverviewTab/BlastGraphLightbox.test.tsx`.
- **2026-06-25** — Design system color tokens (defined in `vendor/ui/styles.css`): green = `--ok` / `--ok-bg`, red = `--crit` / `--crit-bg`, amber/warning = `--warn` / `--warn-bg`. There is NO `--green`, `--red`, or `--amber` — using them silently produces invalid CSS (no color). Spin animation is `ddspin`, not `spin` (`@keyframes ddspin` at line 225). Evidence: `client/src/vendor/ui/styles.css:25-35,225`.
- **2026-07-17** — `agent-browser screenshot --full` (and `document.body.scrollHeight`) does NOT capture this app's actual page content — the app shell keeps `document.body` fixed-height and puts all route content inside an internal `overflow-y:auto` div, so a "full page" screenshot silently truncates at the viewport fold even though the DOM (verified via `document.body.innerText`) has much more below it. To screenshot/inspect content below the fold: find the real scroll container via `getComputedStyle(el).overflowY in ('auto','scroll') && el.scrollHeight > el.clientHeight`, then set its `.scrollTop` directly via `agent-browser eval`, not `agent-browser scroll` (which scrolls the window). Wrap each eval snippet in an IIFE — top-level `const`/`let` collide across successive `eval` calls in the same page session ("Identifier already declared").

## Recurring Errors & Fixes

- **2026-07-11** — A component test that does `vi.mock("@devdigest/ui", () => ({ Skeleton: ... }))` (a full factory replacement, not a partial mock) silently breaks with "Element type is invalid" the moment the component-under-test starts importing any OTHER named export from that barrel (`Button`, `SectionLabel`, `ProgressBar`, `Badge`, `Icon`, …) — those come back `undefined` since the factory only lists `Skeleton`. Fix: every time a component under test picks up a new `@devdigest/ui` import, add a matching stub to that test file's mock factory (plain `<button>`/`<div>`/`<span>` stand-ins are fine; `Icon` needs a `Proxy` since it's accessed as `Icon.<Name>` for arbitrary names). Evidence: `client/src/app/eval/_components/EvalDashboardView.test.tsx`.
- **2026-07-05** — Relative import depth from `pulls/[number]/_components/<Tab>/` to `src/lib/` is SEVEN levels up (`../../../../../../../lib/hooks/brief`), not five — plan docs and intuition routinely undercount because of the `repos/[repoId]/pulls/[number]` nesting. Fix: grep a neighboring component's imports (e.g. `ComposeReviewDrawer`) and copy the depth instead of counting. Evidence: `client/src/app/repos/[repoId]/pulls/[number]/_components/BlastTab/BlastTab.tsx`.

- **2026-06-30** — React warns when `borderColor` (shorthand) and `borderLeftColor` (longhand) appear in the same style object — both affect the same CSS property and React detects the conflict on re-render. Fix: replace `borderColor` with three explicit longhands (`borderTopColor`, `borderRightColor`, `borderBottomColor`) and keep `borderLeftColor` unchanged. Evidence: `client/src/app/repos/[repoId]/pulls/[number]/_components/FindingCard/styles.ts:12-15`.
- **2026-07-15** — A test that finds an expand/collapse target via `title.closest("button")` is asserting an implementation detail (the tag name of whatever owns the click handler), not the behavior — it silently returns `null` if the real component's clickable header is a `<div onClick>` instead of a `<button>` (true for `FindingCard`'s collapsed header). Fix: `fireEvent.click(titleElement)` directly and let the click bubble to whichever ancestor owns the handler — don't hunt for a specific tag. Evidence: `client/src/app/multi-agent/[runId]/_components/MultiAgentResultsView.test.tsx`.
- **2026-07-17** — `StatPoint.label` for the agent-stats trend series arrives as a raw, unformatted `ranAt.toISOString()` from the server (`server/src/modules/agent-performance/service.ts:247`) — by design the server doesn't format it, so the client must. `StatsTab`'s `TrendList` was rendering `pt.label` verbatim (`fontFamily:"monospace"`), showing raw `2026-07-16T11:27:37.309Z` strings in the UI. Fix: added `formatTrendLabel()` (short day+month+HH:mm via `Intl.DateTimeFormat`). Pinned `timeZone: "UTC"` explicitly on both the date and time formatters rather than using the runtime default — the default timezone differs between a server-rendered pass (UTC) and a browser's local timezone, so an unset `timeZone` risks a hydration mismatch (or just a confusingly-different label) the moment any part of this ever runs during SSR. Evidence: `client/src/app/agents/[id]/_components/AgentEditor/_components/StatsTab/StatsTab.tsx` (`formatTrendLabel`).

## Session Notes

### 2026-07-13 (Trend chart tooltip — Phase 6 addendum)
- Extended `@devdigest/ui`'s `LineChart` (recharts) with an optional `points` metadata prop and a Recharts-native tooltip (`ChartTooltip`), rather than reusing the generic hover `Tooltip` wrapper component — the chart's `Line`s render `dot={false}` (no discrete per-point DOM marker to wrap), so Recharts' own `<Tooltip content={...}>` fit the existing render without adding dot markers just to enable the wrapper.
- Wired `AgentEvalDetailView.tsx` to pass formatted timestamp + `v{agent_version}`/"—" + `formatCost(cost_usd)` into the chart; also added an explicit `filteredTrend.length >= 2` guard around the whole chart card (previously the card rendered unconditionally whenever any batch existed — a 0/1-point trend produced a degenerate chart, not a crash, since this is the recharts-backed `LineChart`, not the raw-SVG `Sparkline` that has the real length-1 NaN bug documented below).

### 2026-07-07 (Onboarding Tour UI)
- Built the tour page `repos/[repoId]/onboarding` (multi-agent /implement, tasks T12/T13/T16): five collapsible cards + sticky scroll-spy nav + header, hand-rolled SVG architecture diagram (bounded BFS, max n²+n iterations against cyclic data), blob links `target=_blank`, clipboard copy/share, first-visit "Generate tour" CTA that POSTs only on explicit click (never auto-POST on mount — avoids un-consented LLM spend and a naked 422 when no model is selected), degraded badge via `role="status"` + text (not colour-only).
- New `onboardingTour` i18n namespace — `onboarding.json` belongs to the add-repo wizard; nav item + path-specific `activeKeyFor` so the two `/onboarding`-suffixed routes don't cross-highlight. 11 component tests from AC observables; 129 client tests green.

### 2026-07-07 (Project Context — Edit & Preview drawer)
- Two spec deltas implemented (multi-agent /implement): Project Context page gained a Preview/Edit toggle (`textarea` seeded from the preview read, `role="alert"` ephemerality warning shown only in Edit mode, `aria-live` save status with 2.5s auto-clear, tab resets to Preview on doc switch); both editor Context tabs' preview replaced with a vendor `Drawer` + `SafeMarkdown` (AC-27). `useSaveDocument` does `setQueryData` (instant) then invalidates preview + discovery (token/size refresh, AC-31).
- Design choices were cross-checked against the instructor's `lesson-5-lab/sdd-finish` branch; adopted its Drawer + UX-status patterns, rejected its unsanitized `Markdown` renderer and its missing discovery invalidation (spec violations there) — see the plan's "Reference comparison" section.

### 2026-07-07 (Project Context Folder)
- Built the Project Context UI (multi-agent /implement run): `/project-context` two-pane page (discovery list + SafeMarkdown preview + agent/skill attach checkboxes), Context tabs in AgentEditor and SkillEditor (SkillsTab-style rows, `⠿` drag handle, token-total footer + untrusted-block note, SkillEditor adds a derived "SERIALIZES AS" block), TanStack hooks in `lib/hooks/project-context.ts` with cross-invalidation so page↔tab attach stays consistent, run-trace drawer specs label. `repoId` from `useActiveRepo()` is threaded into preview and set-mutations (multi-repo determinism fix).
- T13 turned out to be a near no-op: `TraceBody` already rendered `prompt_assembly.specs` via `PromptBlock`'s inert `<pre>` — only the i18n label and tests were new.

### 2026-07-06 (Overview compact summary + focus trap)
- Overview tab reworked: `BlastRadiusSection` no longer renders the full `BlastRadiusView` — replaced with a compact 4-count stat row (`blastCounts` from `../BlastRadius/helpers.js`) plus a "View full Blast Radius" button; navigation threaded as `onGoToBlast: () => void` from `page.tsx` (`setTab("blast")`) → `OverviewTab` → section. `repoFullName`/`headSha` props removed from that chain (served only the removed `BlastRadiusView`); `summary.goToTab` key added to `blast.json`. `BlastTab/` and `BlastRadius/` untouched.
- `BlastGraphLightbox` gained a focus trap: initial focus moves to the close button on mount; Tab/Shift+Tab cycles within the dialog (vanilla `querySelectorAll`, no deps). 69 client tests green.

### 2026-07-06 (Blast Radius v2)
- Added Blast Radius section to the Overview tab (HW04): `OverviewTab/BlastRadiusSection.tsx` (reuses `BlastRadiusView` + `usePrBlast` — same `["blast", prId]` query key as BlastTab, so no extra fetch), `PriorPrsAccordion.tsx` (renders null when empty), `prior_prs` on the `BlastRadius` contract (lockstep vendor edit), `priorPrs.*`/`graph*` keys in `blast.json`, `repoFullName`/`headSha` threaded from `page.tsx` through `OverviewTab`.
- BONUS: `BlastGraphLightbox.tsx` — portal + fixed overlay + ESC-close dialog with a hand-rolled layered SVG graph (symbols/callers/endpoints columns, cubic-bezier edges); edges derived per-`DownstreamImpact` so each symbol links only to its own callers/endpoints (no d3 dependency added). Graph button lives in `SectionLabel`'s `right` prop.
- 65 client tests green (7 new); caller click opens `githubBlobUrl` in a new tab — DiffTab still has no `?file=`/`?line=` deep-linking.

### 2026-07-05 (Blast Radius)
- Built Blast Radius UI (L04): `BlastTab` component (Skeleton/ErrorState/degraded Badge with `--warn` tokens, reuses existing `BlastRadiusView` with `onWhy` repurposed to open `githubBlobUrl` in a new tab), `Zap` tab entry in `PrDetailHeader`, `tab === "blast"` branch in `page.tsx`, `tab.*` i18n keys in `blast.json`. PR detail tabs need only two touch points (header tabs array + page branch) — no VALID_TABS gate, unlike AgentEditor. 58 client tests green (4 new).

### 2026-06-30
- Analyzed reference build vs lesson/04 branch; copied missing `_components` (ComposeReviewDrawer, WhyTimelineDrawer, ConformanceTab, PrBriefCard, BlastRadius, SmartDiffViewer) and missing hooks (brief.ts, compose.ts, conformance.ts).
- Used user's enhanced SmartDiffViewer (`allFindings` + `onNavigateToFinding`) over reference's simpler version; updated DiffTab to import from `@/components/SmartDiffViewer` and pass the new props; page.tsx wired `allFindings` + `onNavigateToFinding` → DiffTab.
- Wired `IntentCard` into `OverviewTab` (component existed but was never rendered). Fixed `PrDetailHeader` to add `onComposeOpen` + Conformance tab. Extracted `CodeLine` from `DiffViewer.tsx` into `diff-viewer/CodeLine/`.
- All 22 client tests + 145 server tests pass; TypeScript clean.

### 2026-06-25 (Smart Diff)
- Built Smart Diff UI (L03): `lib/hooks/smartDiff.ts` (`useSmartDiff`), `components/SmartDiffViewer/` (file-list viewer — NOT a code diff viewer — grouped by core/wiring/boilerplate with finding badges and role accordion), `DiffTab` updated with smart/original toggle, `page.tsx` wired `useSmartDiff` + `navigateToFinding`.

### 2026-06-25
- Built Intent Layer UI (L03): `lib/hooks/intent.ts` (usePrIntent + useClassifyIntent), `components/IntentCard/` (skeleton/empty/filled states, `--ok`/`--crit`/`--warn` color tokens, `ddspin` animation), `OverviewTab` updated to accept `prId` and render IntentCard above PR body.

### 2026-06-18
- Built Skills UI (L02): `lib/hooks/skills.ts`, `/skills` page + SkillsListView + SkillCard + ImportDrawer, `/skills/[id]` + SkillEditor with Config/Preview/Versions/Stats tabs, AgentEditor SkillsTab (HTML5 DnD reorder, checkbox link/unlink), nav SKILLS LAB section, i18n keys.
- Skills tab added to AgentEditor — both `constants.ts` (TABS) and `page.tsx` (VALID_TABS) updated.

## Open Questions

- **2026-07-17** — Accept-rate is still rendered with THREE different color-threshold policies across the app after the `AcceptRateGauge`→`CircularScore` consolidation in StatsTab: `CircularScore` (`>=75` ok / `>=50` warn / else crit, 0-100 scale), `AgentPerfTable.tsx`'s `AcceptRateCell` (`>=0.7` ok / `>=0.4` warn / else crit, 0..1 scale, glyph-only — no ring), and `SummaryCards`' `StatCard` badge (reuses `CircularScore`, so already consolidated with the first). Whether `AcceptRateCell`'s table-glyph treatment should also switch to `CircularScore` (and if so, at what size for a dense table row) or is intentionally left glyph-only for table density reasons, is unresolved — flag before touching `AgentPerfTable.tsx`'s accept-rate column. Evidence: `client/src/app/agent-performance/_components/AgentPerfTable.tsx`.
